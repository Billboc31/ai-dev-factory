## Output précédent

Plan produit dans `runs/T010/plan.md`. Voici le résumé :

---

## Plan T010 — Ce qui va changer

**Un seul fichier de code modifié : `tools/agent_runner/run_step.py`**

### 4 ajouts dans `run_step.py`

1. **`import datetime`** — manquant aujourd'hui.

2. **`_log_runtime_step(ticket_id, message)`** — même contrat que le `_log_runtime` de `run_ticket.py`, écrit dans le même `runtime.log`. Les deux scripts cohabitent sans conflit (entrées horodatées).

3. **`_compute_attempt_number(run_dir, step)`** — compte les fichiers existants `<step>-attempt-*.md` et retourne `len + 1`. Simple, robuste aux trous.

4. **`_write_prompt_snapshot(...)`** — écrit `runs/TXXX/prompts/<step>-attempt-N.md` avec un frontmatter YAML (ticket, step, attempt, timestamp, canonical, extra_context) suivi du prompt effectif complet.

### Intégration dans `main()`

Dans le bloc `if args.exec_cmd:`, **après** la construction de `effective_prompt` et **avant** `execute_external_command` :

```python
snapshot_path = _write_prompt_snapshot(...)
_log_runtime_step(ticket_id, f"prompt-snapshot: {snapshot_path}")
print(f"prompt snapshot: {snapshot_path}")
```

Le snapshot est donc écrit même si l'external command échoue.

### Documentation

Section **Runtime prompt snapshots** ajoutée dans `tools/agent_runner/README.md`.

### Pas touché

- `run_ticket.py` — aucun changement
- `prompts/` — aucun changement
- Aucune autonomie ajoutée

---

## Review

Now I have everything I need. Let me write the full review.

---

# Review T010 — Runtime prompt snapshot

**Objet :** Plan `runs/T010/plan.md`
**État workflow :** `PLAN_REVIEW_NEEDED`
**Reviewer :** Claude Sonnet 4.6
**Date :** 2026-05-10

---

## Contexte de la review

Le branch `ticket/T010-runtime-prompt-snapshot` est au stade **plan** — aucun code n'a encore été écrit. Cette review porte donc sur le plan produit par le planner, vérifié contre le ticket T010 et l'état actuel de `run_step.py`.

---

## Checklist T010

### Le snapshot contient exactement le prompt envoyé

**Vérification :** Le plan place le snapshot **après** la construction de `effective_prompt` (lignes 221-232 de `run_step.py`) et **avant** `execute_external_command()`.

```python
# État actuel run_step.py L233 — point d'intégration identifié
stdout, stderr, return_code = execute_external_command(args.exec_cmd, effective_prompt)
```

Le plan insère le snapshot entre les deux blocs. `effective_prompt` est la variable exacte passée à `execute_external_command` — c'est bien le prompt envoyé. ✅

---

### Le snapshot est créé avant l'exécution du LLM

**Vérification :** Explicitement affirmé dans le plan :

> "Le snapshot est donc écrit même si l'external command échoue."

Séquence prévue :

1. Construction `effective_prompt`
2. `_write_prompt_snapshot(...)` ← snapshot ici
3. `_log_runtime_step(...)` ← log
4. `execute_external_command(...)` ← LLM

✅ Ordre correct et robuste aux échecs d'exécution.

---

### Les attempts sont correctement numérotées

**Vérification :** Fonction `_compute_attempt_number(run_dir, step)` — compte les fichiers `<step>-attempt-*.md` existants dans `runs/TXXX/prompts/` et retourne `count + 1`.

Comportement :
- Premier appel step=planner → `planner-attempt-1.md`
- Deuxième appel (après PLAN_FIX_REQUIRED) → `planner-attempt-2.md`
- Chaque step a son propre compteur indépendant

Mécanisme simple, robuste aux trous de numérotation. ✅

---

### Les fix loops T009 sont couverts

**Vérification :** Le contexte injecté par T009 (via `--extra-context-file`) est intégré dans `effective_prompt` **avant** le snapshot :

```python
# run_step.py L228-232 — le contexte T009 est dans effective_prompt
effective_prompt = (
    prompt_content
    + "\n\n---\n\n## Contexte de retry injecté par run_ticket.py\n\n"
    + extra_content
)
```

Le snapshot capture donc le prompt complet avec contexte injecté. Le frontmatter YAML prévu inclut un flag `has_extra_context` (boolean) qui permet de distinguer les executions normales des fix loops.

✅ Fix loops T009 entièrement couverts.

---

### Les chemins sont loggués clairement

**Vérification :** Le plan prévoit :

```python
_log_runtime_step(ticket_id, f"prompt-snapshot: {snapshot_path}")
print(f"prompt snapshot: {snapshot_path}")
```

Double traçabilité : `runtime.log` + stdout. Cohérent avec les patterns existants (`stdout written to:`, `stderr written to:`). ✅

---

### Aucune autonomie dangereuse ajoutée

**Vérification :** Le plan énumère explicitement tout ce qui est **hors scope** :

- pas de replay automatique
- pas de resume automatique
- pas de comparaison automatique
- pas de merge automatique
- pas de PR automatique
- pas de watcher permanent

`_write_prompt_snapshot` est une opération purement passive de persistence. Aucun chemin de feedback vers le LLM ou vers le state machine. ✅

---

### Prompts canoniques inchangés

**Vérification :** Git diff `main...HEAD --name-only` montre uniquement des modifications dans `runs/T010/`. Le répertoire `prompts/` est intact.

Le plan précise explicitement : `prompts/ — aucun changement`. ✅

---

### README à jour

**Vérification :** Le plan prévoit une section **"Runtime prompt snapshots"** dans `tools/agent_runner/README.md`. Actuellement le README ne documente pas les snapshots.

Couverture prévue : convention de nommage, localisation des fichiers, structure frontmatter, exemple avec fix loop. ✅ (à implémenter)

---

## Points d'attention pour le coder

### Point 1 — Capture du retour de `ensure_run_tree`

Actuellement dans `main()` :
```python
ensure_run_tree(ticket_id)  # retour ignoré
```

Pour passer `run_dir` à `_compute_attempt_number` et `_write_prompt_snapshot`, le coder devra capturer :
```python
run_dir = ensure_run_tree(ticket_id)
```

Le plan ne l'énonce pas explicitement mais c'est la solution naturelle.

### Point 2 — Répertoire `prompts/` déjà créé

`"prompts"` est dans `RUN_SUBDIRS` (`run_step.py:24`). Le répertoire `runs/TXXX/prompts/` est donc déjà créé par `ensure_run_tree()`. Aucune création supplémentaire nécessaire dans `_write_prompt_snapshot`.

### Point 3 — Frontmatter YAML

Le plan liste les clés (ticket, step, attempt, timestamp, canonical, extra_context) sans spécifier le format exact. Le coder devra choisir une représentation cohérente avec les autres artefacts existants. Suggestion : écriture directe en f-string plutôt qu'une dépendance `pyyaml`.

---

## Synthèse

| Critère | Statut |
|---------|--------|
| Snapshot contient le prompt exact | ✅ Plan correct |
| Snapshot avant exécution LLM | ✅ Ordre explicite |
| Numérotation des attempts | ✅ Mécanisme clair |
| Fix loops T009 couverts | ✅ effective_prompt capturé |
| Chemins loggués | ✅ runtime.log + stdout |
| Aucune autonomie dangereuse | ✅ Scope strictement limité |
| Prompts canoniques inchangés | ✅ Vérifiés sur git |
| README prévu | ✅ Section spécifiée |

Le plan est complet, précis, et couvre tous les critères d'acceptation du ticket T010. Les trois points d'attention sont mineurs et n'empêchent pas l'implémentation.

---

## Verdict

**PLAN_APPROVED**

---

## Instructions de fix

