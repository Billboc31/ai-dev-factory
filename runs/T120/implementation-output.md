Fix applied and committed. `prompts/generic/planner.md` is now identical to `main` — the out-of-scope restructuring has been removed from this PR. The four T120 implementation files remain untouched.

The planner prompt improvement should be tracked in a separate ticket to get its own plan, review, and merge cycle.
