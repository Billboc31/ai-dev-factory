Plan written to `runs/T221/plan.md` — four mandatory headings in order, no status-report prose. It removes the two `break` statements at run_daemon.py:1432 and run_daemon.py:1501, registers `MAX_ISSUES_INTAKED_PER_POLL` / `MAX_PARALLEL_TICKET_INTELLIGENCE` / `MAX_PARALLEL_READINESS` in `runtime_settings.py`, parallelises `poll_ticket_pipeline` with two bounded `ThreadPoolExecutor`s, and adds `claim_intelligence` / `claim_readiness` conditional-UPDATE helpers in `ticket_pipeline.py` (reusing the existing `analysis_status='queued'` / `readiness_status='queued'` markers so no schema migration is needed). Test additions cover 20-issue batch intake, idempotent re-polls, race-condition claim safety for both stages, parallelism bounds, invalid-setting fallback to 1, and an explicit assertion that `MAX_WORKERS` execution concurrency is untouched.
