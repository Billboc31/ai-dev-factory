All failures are in the same pre-existing sets (`test_daemon_*`, `test_run_daemon`, `test_ticket_timeline`). The 2-test variance between runs is natural flakiness in those unrelated test files — nothing from our changes.

The implementation is fully verified and ready to commit.
