Plan written to `runs/T216/plan.md`. It scopes the work to adding three explicit regression tests in `tests/test_control_api_settings.py` (empty-table, env-source, db-source-after-put) and only patching `tools/agent_runner/runtime_settings.py` if reproduction shows the resolver is actually dropping entries — the current `list_effective_settings()` (line 316-318) already iterates `SETTING_SPECS`, so the bug as described may not reproduce against the current code, which the plan calls out as an explicit assumption.
