fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0
pytest>=8.0.0
pyyaml>=6.0
# Optional Postgres backend for the runtime store (RUNTIME_DB_BACKEND=postgres).
# psycopg (v3) binary wheel — no system libpq build needed.
psycopg[binary]>=3.1.0
